{
  "name": "@tjakoen/crumb",
  "version": "0.0.0",
  "private": true,
  "description": "CRUMB — the GRAIN stack's guided-tour / demo-mode / AI-review layer. Tours-as-markdown rendered as a guided projection that never writes (PROOF's twin). SCAFFOLD ONLY — see PLAN.md; build is effort B, gated.",
  "type": "module",
  "license": "Apache-2.0",
  "author": "Tjakoen Stolk <tjakoen.s@gmail.com> (https://tjakoen.github.io)",
  "homepage": "https://tjakoen.github.io/crumb",
  "dependencies": {
    "@tjakoen/grain": "workspace:*",
    "@tjakoen/mill": "workspace:*"
  },
  "scripts": {
    "check": "echo 'crumb: not built yet (scaffold) — see PLAN.md'",
    "test": "echo 'crumb: no tests yet (scaffold) — see PLAN.md'"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^5.8.0"
  }
}
