{
  "name": "@tjakoen/grain-monorepo",
  "private": true,
  "type": "module",
  "description": "GRAIN monorepo — the AI-interaction design-system family (grain, mill, proof, crumb) as one bun workspace. Consumers pin this repo; internal deps resolve via workspace:*.",
  "license": "Apache-2.0",
  "author": "Tjakoen Stolk <tjakoen.s@gmail.com> (https://tjakoen.github.io)",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^5.8.0"
  }
}
